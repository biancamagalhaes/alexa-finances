{
  "name": "@alexa-finances/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --import tsx --watch src/server.ts",
    "start": "node --env-file=.env --import tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@alexa-finances/database": "file:../../packages/database",
    "@alexa-finances/market-data": "file:../../packages/market-data",
    "@alexa-finances/portfolio-domain": "file:../../packages/portfolio-domain",
    "fastify": "^5.0.0",
    "tsx": "^4.19.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
