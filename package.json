{
  "name": "alexa-finances",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start:discloud": "NODE_ENV=production PORT=${PORT:-8080} HOST=${HOST:-0.0.0.0} node --import tsx apps/api/src/server.ts",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "tsx": "^4.19.0"
  }
}
